# shader-announce OSD behavior.
# debug=no: chain changes log to the terminal only; on-screen feedback
#           comes from the ALT+1..7 bindings when you switch manually.
# debug=yes: every chain change (including automatic profile swaps)
#            announces itself in the OSD.
debug=no
